# Python Requirements

This folder contains Python requirement textfiles that are used to install 
all the packages needed to use on:

a) Line Stations (Teststand and SPS) -> line_stations.txt
b) Development Station (TSDEV)       -> tsdev_pc.txt
c) Production Server (sv-prod)       -> production_server.txt
d) general snapshot (for reference)  -> snapshot.txt

